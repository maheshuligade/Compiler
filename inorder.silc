typedef bst{
	integer data;
	bst L;
	bst R;
}
decl
	integer i,inorder(bst G);
	bst insert(bst G);
enddecl
bst insert(bst G)
{
	decl
		integer a;
	enddecl

	begin

	 return G;
	end

}

integer inorder(bst G)
{
	decl
		integer a;
	enddecl


	begin
		if(G == -1) then
			
		else 
			write(G.data);
			write(G.R);
			write(G.L);
			write(G.L.L);
			write(G.L.R);
			write(G);
		endif;
		return 0;
	end
}

	
integer main(){
decl
	integer a;
	bst A;
enddecl
begin
	
	A = allocate();
	A.L = allocate();
	A.R = allocate();
	A.data = 5;
	A.L.data = 4;
	A.R.data = 6;
	a = inorder(A);
	
	return 0;
end
}